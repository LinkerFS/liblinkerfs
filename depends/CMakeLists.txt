if (BUILD_SHARED_LIBS)
    set(CMAKE_FIND_LIBRARY_SUFFIXES ".so" ".dll.a")
else ()
    set(CMAKE_FIND_LIBRARY_SUFFIXES ".a" ".lib")
endif ()
include(linkerfs.cmake)

if (LIBLINKERFS_ENABLE_UDF)
    include(libudfread.cmake)
endif ()